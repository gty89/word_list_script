This script fetches GRE vocabulary data from the Merriam–Webster Dictionary API, processes the response into a structured format locally, and exports it as a CSV file for database import.

The exported dataset is intended for a downstream application: a GRE word pairing game, where words, meanings, synonyms, and antonyms can be used to generate matching or quiz-based gameplay.
